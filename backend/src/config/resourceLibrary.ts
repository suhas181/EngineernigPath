/**
 * Backward-compatibility wrapper exporting unified resource library from modular architecture.
 */
export {
  ALL_RESOURCES,
  resolveResources,
  LibraryResource,
  ResourceLanguage,
  ResourceType,
  ResourceLevel,
  ResourceStage,
} from '../resources';
